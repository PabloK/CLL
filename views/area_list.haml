.col5
  .blue_row
    %h2 Kompetensområden
  %ul.item-list
    -@areas.each do |area|
      %li{:class => cycle('odd','even')}=area.name
  %button.btn{:type => "link", :onClick => "window.location.href='/admin/area'", :tabindex => 1} Lägg till kompetensområde
